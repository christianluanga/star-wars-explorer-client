export type Character = {
    name: string
    height: string
    mass: string
    gender: string
    homeworld: string
}

export type Film = {
    title: string
    director: string
    release_date: string
    opening_crawl: string
}